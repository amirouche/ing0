[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ingzero"
version = "0.2.0"
description = "diverse productivity tools"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "lxml>=6.0.2",
    "requests>=2.32.5",
]

[project.scripts]
ing0 = "ing0:main"
